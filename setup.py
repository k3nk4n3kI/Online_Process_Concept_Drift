from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Contains all the necessary files and data for the project of Online Process Concept Drift.',
    author='Lars_Gsänger',
    license='',
)
